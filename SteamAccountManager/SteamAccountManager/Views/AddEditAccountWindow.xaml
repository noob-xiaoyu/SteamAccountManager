<!-- Views/AddEditAccountWindow.xaml -->
<Window x:Class="SteamAccountManager.Views.AddEditAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:helpers="clr-namespace:SteamAccountManager.Helpers"
        Title="{Binding WindowTitle}" Height="380" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#2D2D30">
    <Window.Resources>
        <helpers:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <!-- 新增 Prime 行 -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 样式定义 -->
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,0,10,0"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="0,5"/>
            </Style>
            <Style TargetType="ComboBox">
                <Setter Property="Margin" Value="0,5"/>
            </Style>
        </Grid.Resources>

        <TextBlock Text="账号:" Grid.Row="0" Grid.Column="0" Margin="0,6,10,6" VerticalAlignment="Stretch"/>
        <TextBox Text="{Binding Account.Username, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="0,5,0,5"/>

        <TextBlock Text="密码:" Grid.Row="1" Grid.Column="0" Margin="0,6,10,6" VerticalAlignment="Stretch"/>
        <TextBox Text="{Binding Account.Password, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="0,5,0,6"/>

        <TextBlock Text="SteamID64:" Grid.Row="2" Grid.Column="0" Margin="0,6,10,6" VerticalAlignment="Stretch"/>
        <TextBox Text="{Binding Account.SteamID64, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="0,4,0,5"/>

        <TextBlock Text="备注:" Grid.Row="3" Grid.Column="0" Margin="0,6,10,6" VerticalAlignment="Stretch"/>
        <TextBox Text="{Binding Account.Nickname, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="1" Margin="0,5,0,5"/>

        <!-- [核心改动] 添加状态选择框 -->
        <TextBlock Text="封禁状态:" Grid.Row="4" Grid.Column="0"/>
        <ComboBox Name="StatusComboBox" SelectedItem="{Binding Account.Cs2BanStatus}" Grid.Row="4" Grid.Column="1">
            <sys:String>正常</sys:String>
            <sys:String>冷却</sys:String>
            <sys:String>VAC</sys:String>
        </ComboBox>

        <TextBlock Text="冷却时间:" Grid.Row="5" Grid.Column="0" Visibility="{Binding ElementName=StatusComboBox, Path=SelectedItem, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=冷却}"/>
        <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal"  Visibility="{Binding ElementName=StatusComboBox, Path=SelectedItem, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=冷却}">
            <TextBox Text="{Binding CooldownDays, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}" Width="50" Margin="10,5,5,5"/>
            <TextBlock Text="天" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding CooldownHours, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}" Width="50" Margin="10,5,5,5"/>
            <TextBlock Text="小时" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        </StackPanel>

        <TextBlock Text="Prime 状态:" Grid.Row="6" Grid.Column="0"/>
        <ComboBox SelectedItem="{Binding Account.Cs2PrimeStatus}" Grid.Row="6" Grid.Column="1">
            <sys:String>是</sys:String>
            <sys:String>需升级</sys:String>
        </ComboBox>

        <!-- 按钮 -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0" Grid.Column="1" Width="170">
            <Button Content="确定" Width="80" Height="30" IsDefault="True" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Content="取消" Width="80" Height="30" IsCancel="True" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>